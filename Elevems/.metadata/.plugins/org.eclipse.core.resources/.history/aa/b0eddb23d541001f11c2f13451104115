package com.example.demo.dto;

import com.example.demo.entities.Eleve;

public class EleveMapper {

/*public static EleveDTO EntitytoDTOE(Eleve eleve) {
    if (eleve == null) {
        return null;
    }

    return EleveDTO.builder()
                     .eleveId(eleve.getBillId())
                     .customerId(bill.getCustomerId())
                     .amount(bill.getAmount())
                     .build();
}

public static BillDTO EntitytoDTOC(Bill bill, CustomerDTO customerDTO) {
    if (bill == null) {
        return null;
    }

    return BillDTO.builder()
                     .billId(bill.getBillId())
                     .customerId(String.valueOf(customerDTO.getCustomerId())) // Convert int to String if necessary
                     .amount(bill.getAmount())
                     .build();
}

public static Bill DTOtoEntity(BillDTO dto) {
    if (dto == null) {
        return null;
    }

    return Bill.builder()
                     .billId(dto.getBillId())
                     .customerId(dto.getCustomerId())
                     .amount(dto.getAmount())
                     .build();*/
}